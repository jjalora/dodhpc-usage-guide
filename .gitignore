# Personal configuration (written by `make configure`)
config.mk

# OS noise
.DS_Store

# Run artifacts
logs/
outputs/
smoke_output/
wandb_offline_sync/
__pycache__/

# Smoke-test job id (written by `make smoke`, read by `make smoke-wait`)
.hpc_smoke_job
